# پروفایل‌های Xray برای WS

## نتیجه درباره Mux

طبق مستندات رسمی Xray، Mux برای کم‌کردن تأخیر handshake طراحی شده، نه افزایش throughput؛ برای دانلود، ویدئو و speed-test معمولاً اثر منفی دارد. Mux فقط در کلاینت تنظیم می‌شود و سرور خودکار آن را می‌پذیرد. لینک استاندارد VLESS نیز تنظیم Mux را حمل نمی‌کند.

### پروفایل پیشنهادی اصلی — بیشترین دانلود/آپلود

```json
"mux": {
  "enabled": false,
  "concurrency": -1,
  "xudpConcurrency": -1
}
```

برای دانلود منیجر، ویدئو، آپلود سنگین و تست سرعت از این حالت استفاده کنید. دانلود منیجر خودش چند Range connection باز می‌کند؛ هرکدام یک WS مستقل و یک congestion window مستقل دارند.

### پروفایل اختیاری Web-Boost — فقط برای لود صفحات در پینگ بالا

```json
"mux": {
  "enabled": true,
  "concurrency": 4,
  "xudpConcurrency": -1,
  "xudpProxyUDP443": "reject"
}
```

این حالت handshake چند اتصال کوچک را کم می‌کند، ولی چند زیرجریان را روی carrier مشترک می‌گذارد. اگر دانلود یا ویدئو کند شد، فوراً Mux را خاموش کنید. مقدار 4 عمداً پایین است تا در بار زیاد Xray چند carrier بسازد و همه‌چیز در یک TCP قفل نشود.

## تنظیمات مشترک WS

- پورت بیرونی: `443`
- Transport: `ws`
- TLS: روشن
- ALPN: `http/1.1`
- Path: `/ws/UUID?ed=4096`
- Per-message compression: خاموش

برای تصمیم نهایی، هر دو پروفایل را روی شبکه خودتان A/B تست کنید؛ کیفیت مسیر ISP و CDN تعیین می‌کند Web-Boost واقعاً بهتر باشد یا نه.
