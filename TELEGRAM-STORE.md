# راهنمای بات فروش تلگرام

بات تلگرام این نسخه یک فروشگاه کامل سرویس VLESS + WebSocket است. کاربران عادی با `/start` وارد فروشگاه می‌شوند و ادمین‌ها علاوه بر منوی کاربر، با `/admin` به پنل مدیریت فروش دسترسی دارند.

## امکانات کاربر

- نمایش «سرویس‌های من» با وضعیت، حجم مصرف‌شده، حجم باقی‌مانده و تاریخ پایان
- خرید سرویس جدید از کاتالوگ قیمت
- تمدید سرویس قبلی؛ حجم استفاده‌نشده حفظ و به حجم جدید اضافه می‌شود و روزهای جدید از انتهای اعتبار فعلی محاسبه می‌شوند
- دریافت کانفیگ کامل تکی VLESS
- دریافت لینک Subscription هر سرویس
- دریافت Subscription یکپارچه همه سرویس‌های همان کاربر
- کیف پول، شارژ کارت‌به‌کارت و استفاده از کد هدیه
- کد تخفیف برای خرید یا تمدید مستقیم
- راهنمای اتصال Android، iOS و Windows

تمام سرویس‌های خریداری‌شده با `vless-ws`، TLS، پورت عمومی 443، مسیر WS کامل و Early Data ساخته می‌شوند. کاربر هیچ فیلد شبکه‌ای را دستی تنظیم نمی‌کند.

## جریان پرداخت کارت‌به‌کارت

1. کاربر پلن را انتخاب می‌کند و در صورت نیاز کد تخفیف وارد می‌کند.
2. بات مبلغ دقیق، شماره کارت، نام صاحب کارت و کد سفارش را نمایش می‌دهد.
3. کاربر تصویر یا PDF رسید را برای بات می‌فرستد.
4. رسید همراه مبلغ، Telegram ID و کد سفارش برای همه ادمین‌ها ارسال و در صف پرداخت‌های در انتظار ذخیره می‌شود.
5. ادمین «تایید» یا «رد» را می‌زند.
6. تایید به‌صورت idempotent انجام می‌شود: حتی اگر چند ادمین روی تایید بزنند، موجودی فقط یک‌بار شارژ یا سرویس فقط یک‌بار ساخته می‌شود.

بات فقط رسید را برای بررسی ارائه می‌کند و خودش تراکنش بانکی را تایید نمی‌کند. ادمین باید مبلغ و واریز واقعی را در حساب بانکی کنترل کند؛ تصویر رسید به‌تنهایی اثبات قطعی پرداخت نیست.

## پنل ادمین تلگرام

- صف رسیدهای در انتظار و نمایش مجدد فایل رسید
- تایید یا رد پرداخت
- ساخت کد هدیه شارژ کیف پول با مبلغ، سقف استفاده و انقضا
- ساخت کد تخفیف درصدی یا مبلغ ثابت با سقف استفاده و انقضا
- تنظیم شماره کارت و نام صاحب کارت
- مشاهده پلن‌ها و آمار فروشگاه

ورود: `/admin` یا دکمه «پنل مدیریت فروش» در منوی اصلی ادمین.


## قیمت‌های پیش‌فرض

| پلن | حجم | اعتبار | قیمت |
|---|---:|---:|---:|
| اقتصادی | 30GB | 30 روز | 79,000 تومان |
| استاندارد | 60GB | 30 روز | 129,000 تومان |
| حرفه‌ای | 100GB | 30 روز | 179,000 تومان |
| دوماهه | 150GB | 60 روز | 289,000 تومان |
| سه‌ماهه | 250GB | 90 روز | 429,000 تومان |
| پرحجم | 500GB | 90 روز | 749,000 تومان |

برای جایگزینی کاتالوگ بدون تغییر کد، متغیر `STORE_PLANS_JSON` را با یک آرایه JSON تنظیم کنید. نمونه:

```json
[
  {"id":"p50","name":"پنجاه گیگ","gb":50,"days":30,"price":110000,"active":true},
  {"id":"p120","name":"صدوبیست گیگ","gb":120,"days":60,"price":240000,"active":true}
]
```

`id` هر پلن باید یکتا و فقط شامل حروف انگلیسی، عدد، `_` یا `-` باشد. قیمت‌ها به تومان هستند.

## متغیرهای محیطی

| متغیر | کاربرد | الزامی |
|---|---|---|
| `TELEGRAM_BOT_TOKEN` | توکن BotFather | بله |
| `TELEGRAM_ADMIN_IDS` | Telegram ID ادمین‌ها، جداشده با کاما | بله برای تایید پرداخت |
| `STORE_CARD_NUMBER` | شماره کارت 16 رقمی؛ از پنل ادمین هم قابل تنظیم است | برای کارت‌به‌کارت |
| `STORE_CARD_HOLDER` | نام صاحب کارت؛ از پنل ادمین هم قابل تنظیم است | برای کارت‌به‌کارت |
| `STORE_NAME` | نام فروشگاه در منوی کاربر | خیر |
| `STORE_SUPPORT_USERNAME` | یوزرنیم پشتیبانی بدون `@` | خیر |
| `STORE_IP_LIMIT` | تعداد IP هم‌زمان هر سرویس فروخته‌شده؛ پیش‌فرض 2 | خیر |
| `STORE_PLANS_JSON` | کاتالوگ قیمت سفارشی | خیر |
| `DATA_DIR` | محل state؛ پیش‌فرض `/data` | بله برای Volume پایدار |
| `RAILWAY_PUBLIC_DOMAIN` | دامنه عمومی برای ساخت لینک‌ها | در Railway بله |

اگر `TELEGRAM_ADMIN_IDS` یا اطلاعات کارت تنظیم نشده باشد، پرداخت کارت‌به‌کارت برای کاربر شروع نمی‌شود و پیام خطای واضح نمایش داده می‌شود.

## ذخیره‌سازی

- سفارش‌ها، کاربران، کیف پول، کدها و تنظیمات فروشگاه: `telegram_store.json`
- کانفیگ‌ها و گروه‌های Subscription: `code_state.json`

هر دو فایل زیر `DATA_DIR` ذخیره می‌شوند. در Railway حتماً Volume دائمی را روی `/data` متصل کنید. فایل فروشگاه با نوشتن اتمیک و permission برابر `0600` ذخیره می‌شود.

## نکات عملیاتی

- بعد از Deploy، ابتدا با اکانت ادمین `/admin` را بفرستید و شماره کارت را تنظیم کنید.
- سپس با یک اکانت غیرادمین، خرید آزمایشی، ارسال رسید، رد و تایید را کامل تست کنید.
- قبل از تایید هر رسید، تراکنش واقعی حساب بانکی و مبلغ سفارش را بررسی کنید.
- حذف یا ویرایش دستی `telegram_store.json` هنگام روشن بودن برنامه انجام نشود.
